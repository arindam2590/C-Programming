#include <stdio.h>

int main() {
    int num;

    // Input from the user
    printf("Enter an integer: ");
    scanf("%d", &num);

    // Print the entered integer
    printf("You entered: %d\n", num);

    return 0;
}
